# Optional Source Watch runtime configuration
SOURCE_WATCH_ENABLED=true
SOURCE_WATCH_WORKER_INTERVAL_SECONDS=60

# Only needed for Facebook Page monitoring through Meta Graph API.
# Keep this token only in server/.env and never expose it to the client.
FACEBOOK_GRAPH_ACCESS_TOKEN=
